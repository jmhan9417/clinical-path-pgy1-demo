import fs from 'node:fs';
const demo=fs.readFileSync(new URL('../demo.html',import.meta.url),'utf8');
const home=fs.readFileSync(new URL('../index.html',import.meta.url),'utf8');
const checks=[];const add=(name,pass)=>checks.push({name,pass:Boolean(pass)});
add('Curated catalog count is 457',home.includes('<b>457</b>&nbsp;clinical cases')&&!/692 (?:clinical cases|decisions)/.test(home));
add('DKA dextrose threshold is below 250',demo.includes('glucose falls below 250 mg/dL')&&!demo.includes('add dextrose when glucose ~200'));
add('DKA resolution uses ketonemia and acidosis',demo.includes('ketonemia and acidosis resolve')&&!demo.includes('anion gap is still open'));
add('DKA potassium threshold is 3.5',demo.includes('K⁺ &lt; 3.5')&&!demo.includes('K⁺ &lt; 3.3')&&!demo.includes('K⁺ ≥ 3.3'));
add('Sepsis actions are parallel',demo.includes('Start resuscitation actions in parallel'));
add('Polymorphic VT prioritizes unsynchronized shock',demo.includes('Immediate high-energy unsynchronized shock'));
add('Beta-blocker poisoning includes high-dose insulin',demo.includes('high-dose regular insulin with dextrose'));
add('Andexanet withdrawal is current',demo.includes('andexanet was withdrawn from the U.S. market in 2025')&&!demo.includes('Andexanet alfa (or 4F-PCC)'));
add('Warfarin target is indication-specific',demo.includes('INR to the indication-specific target'));
add('Kidney estimate follows label/evidence',demo.includes('Use the kidney estimate specified by the drug label'));
add('Enoxaparin does not imply routine anti-Xa adjustment',demo.includes('routine anti-Xa-guided adjustment is not universally recommended'));
add('Vancomycin includes early AUC and Bayesian timing',demo.includes('target exposure within 24–48 hours')&&demo.includes('non–steady-state samples'));
add('Phenytoin correction is only an estimate',demo.includes('This is a screening estimate; obtain a free phenytoin concentration'));
add('Corrected calcium includes ionized-calcium caveat',demo.includes('obtain ionized calcium for acute decisions'));
add('CYP guidance names isoenzyme',demo.includes('itraconazole/clarithromycin/grapefruit inhibit CYP3A'));
add('Vancomycin infusion reaction starts by stopping infusion',demo.includes('Stop the infusion, assess severity and exclude anaphylaxis'));
add('CDI preference hierarchy is current',demo.includes('fidaxomicin over vancomycin while retaining vancomycin as an acceptable alternative'));
add('ASB procedure exception requires mucosal trauma',demo.includes('endoscopic urologic procedure associated with mucosal trauma'));
add('USP CSTD language distinguishes compounding and administration',demo.includes('a CSTD is recommended during compounding and required for administration when the dosage form permits'));
add('Chemotherapy verification does not require two pharmacists',demo.includes('qualified personnel approved by the healthcare setting')&&!demo.includes('two-pharmacist independent verification'));
add('TLS includes G6PD contraindication',demo.includes('Rasburicase is contraindicated in G6PD deficiency'));
add('Highly emetogenic CINV includes olanzapine',demo.includes('NK1 antagonist + 5-HT3 antagonist + dexamethasone + olanzapine')&&!demo.includes('± olanzapine'));
add('ICI colitis uses increase over baseline',demo.includes('≥7 watery stools/day above baseline'));
add('Oral hydromorphone MME factor is 5',demo.includes('Oral hydromorphone 8 mg/day')&&demo.includes('8 × 5 = 40 MME/day'));
add('SGLT2 CKD threshold is explicit',demo.includes('eGFR is at least 20 mL/min/1.73 m²'));
add('Pediatric dosing uses drug-specific maximum',demo.includes('drug- and indication-specific pediatric maximum'));
add('Pregnancy anticoagulation preserves valve nuance',demo.includes('selected mechanical-valve cases require specialist warfarin'));
add('Live-vaccine spacing is route-specific',demo.includes('injectable or intranasal live vaccines'));
add('Federal refill rule separates Schedule V',demo.includes('Schedules III–IV allow no more than five refills'));
add('Buprenorphine rule retains authority and MATE requirements',demo.includes('DEA MATE training requirements remain'));
add('Metformin uses eGFR, not CrCl',demo.includes('metformin is contraindicated below eGFR 30'));
add('PRISMA is reporting, not quality proof',demo.includes('PRISMA checklist used to assess reporting completeness'));
add('CPOE reduces rather than removes errors',demo.includes('reduces many ordering/transcription errors'));
add('MRSA nares limitation is pneumonia-specific',demo.includes('MRSA nasal PCR may support stopping empiric vancomycin for pneumonia'));
add('Culture-directed AKI SBAR stops unnecessary broad therapy',demo.includes('Stop ketorolac, discontinue vancomycin and piperacillin-tazobactam, narrow to ceftriaxone, and monitor renal function and urine output.')&&!demo.includes('hold/re-dose vancomycin by levels')&&!demo.includes('vancomycin level-based holding or redosing are also required'));
add('QI and publication intent do not define research',demo.includes('publication intent alone does not make QI human-subjects research'));
add('Stroke threshold and thrombolytic doses are explicit',demo.includes('Lower BP to <185/110 mm Hg')&&demo.includes('tenecteplase 0.25 mg/kg (max 25 mg)'));
add('Epinephrine uses mg-per-mL concentration language',!demo.includes('1:1000 vs 1:10,000')&&demo.includes('verify epinephrine in mg/mL'));
add('Potassium response is not taught as a fixed rise',!demo.includes('~10 mEq raises serum ~0.1')&&demo.includes('Do not predict a fixed serum rise'));
add('Hypomagnesemia wording avoids fix-first overclaim',!demo.includes('fix Mg first')&&demo.includes('replace magnesium and potassium while evaluating ongoing losses'));
add('Naloxone counseling avoids a rigid MME cutoff',!demo.includes('≈≥50/day')&&demo.includes('MME is a risk estimate, not a rigid cutoff'));
for(const c of checks)console.log(`${c.pass?'PASS':'FAIL'}  ${c.name}`);
const failed=checks.filter(c=>!c.pass);console.log(`\n${checks.length-failed.length}/${checks.length} clinical checks passed`);if(failed.length)process.exit(1);
